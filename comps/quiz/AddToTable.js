


export const AddToTable = ({setShowModal}) => {
    
    return (
        <div >
            <button className="btn"
                type="button"
                onClick={() => setShowModal(true)}
            >Add Blog</button>

        </div>
    )
}
